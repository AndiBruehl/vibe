# VIBE 0.1.78

## Persistent Settings navigation

- Give Settings tabs and profile-look sections stable query-parameter URLs without triggering a page navigation.
- Keep the selected Settings area after a browser refresh, bookmark, or shared internal link.
- Add `tab` and `section` URL parameters for profile, account, appearance general, appearance background, appearance layout, and appearance frame settings.
- Keep Apply, Reset, Restore, and Save actions in place without refreshing the whole Settings route.
- Align the Blocked users back control with the page edge and remove its hover surface.
- Match the home changelog width to the post feed and animate expanding and collapsing smoothly.

## Validation

- `tsc --noEmit`
- `git diff --check`
