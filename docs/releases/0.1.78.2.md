# VIBE 0.1.78.2

## Profile layout alignment

- Align profile actions, links, shoutouts, and milestones with the active header layout.
- Keep Compact profile content left aligned.
- Keep Spotlight profile content centered on every screen size.
- Keep Standard public profiles centered on mobile and left aligned on desktop, while Standard headers with a background remain left aligned.

## Validation

- `tsc --noEmit`
- Targeted ESLint checks for both profile routes and milestone display
- `git diff --check`
