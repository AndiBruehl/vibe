# VIBE 0.1.76

## Profile milestones

- Recalculate milestone progress for public profiles as well as the signed-in profile.
- Display earned milestones on every profile using the profile's Standard, Compact, or Spotlight alignment.
- Preserve the last known profile data when milestone maintenance is temporarily unavailable.
- Keep legacy or missing milestone arrays safe and render an empty state instead of failing.

## Validation

- `tsc --noEmit`
- `git diff --check`
