# VIBE 0.1.79.3

## Delete confirmation

- Add a themed, multilingual confirmation before delete actions are submitted.
- Require an explicit Delete confirmation; Cancel leaves the item unchanged.
- Replace browser confirmation prompts for post, draft, and group deletion.

## Video frame editor polish

- Rename user-facing poster language to Custom frame.
- Add a generous orange drag-and-drop target for custom frame uploads.
- Make the selected-frame action a prominent centered confirmation below the slider.
- Keep custom frame selection available even when a post already contains four media items.

## Validation

- TypeScript check
